/** Mirrors backend `com.bookcloud.smartlibrary.model.LibraryBranch`. */
export interface LibraryBranch {
  id: string;
  name: string;
  address: string;
  openingHours: string;
  createdAt: string | null;
}

